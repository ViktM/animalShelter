@page
@model animalShelter.Pages.Cats.ListModel

@{
    ViewData["Title"] = "Cats";
}
<br/>
<p>
<a asp-page="Create">Create New Cat</a>
</p>
<table class="table table-hover">
    <thead>
    <tr class="table-dark">
        <th>
            @Html.DisplayNameFor(model => model.Cat[0].Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cat[0].Dob)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cat[0].Breed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cat[0].Sex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cat[0].Summary)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Cat)
    {
        <tr class="table-active">
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dob)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Breed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sex)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Summary)
            </td>

            <td>
                <a asp-page="./Edit" asp-route-id="@item.CatID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CatID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CatID">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>